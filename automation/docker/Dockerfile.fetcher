FROM python:3.9-slim
WORKDIR /app
# copy fetcher and requirements (we assume build context is repo root)
COPY automation/fetch_live_data.py /app/
COPY automation/requirements-fetch.txt /app/
RUN pip install --no-cache-dir -r requirements-fetch.txt
ENTRYPOINT ["python", "fetch_live_data.py"]
